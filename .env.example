# URL real del backend (la usa el servidor / el proxy).
# En el navegador las peticiones van a /api/v1 (mismo origen).
NEXT_PUBLIC_API_BASE_URL=https://apidatos.cerebiia.com.co/api/v1
# Opcional: override server-only del destino del proxy
# API_PROXY_TARGET=https://apidatos.cerebiia.com.co/api/v1